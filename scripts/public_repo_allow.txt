# Caminhos isentos do public_repo_check.py — um glob por linha.
# Isenção de CAMINHO INTEIRO é a exceção cara: ela esconde o arquivo todo, não uma linha.
# Prefira `public-ok: <motivo>` na linha. Toda entrada aqui leva o motivo escrito ao lado.

# Fixtures do redator de segredos: as chaves de exemplo SÃO o material de teste,
# e são exemplos publicados (AWS docs) ou sequências óbvias, nunca credencial real.
plugins/project-skills/lib/test_journal.py
